---
description: "Playwright e2e, MSW mocks, test config (with-lingui)."
globs: manage/e2e/**/*.ts,manage/e2e/**/*.tsx,manage/src/mocks/**/*.ts,manage/playwright.config.ts
alwaysApply: false
---

# with-lingui · Testing（薄规则）

**唯一细则来源**：

`manage/.github/instructions/testing.instructions.md`

编辑符合 `globs` 的文件时，遵守该文件中的约定；冲突以 `.instructions.md` 为准。

`@manage/.github/instructions/testing.instructions.md`
