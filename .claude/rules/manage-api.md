---
description: "API clients, Zod schemas, HTTP, MSW handlers (with-lingui)."
globs: manage/src/api/**/*.ts,manage/src/utils/http.ts,manage/src/mocks/handlers/**/*.ts,manage/src/mocks/createHandler.ts
alwaysApply: false
---

# with-lingui · API（薄规则）

**唯一细则来源**：

`manage/.github/instructions/api.instructions.md`

编辑符合 `globs` 的文件时，遵守该文件中的约定；冲突以 `.instructions.md` 为准。

`@manage/.github/instructions/api.instructions.md`
