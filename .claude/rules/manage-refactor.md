---
description: "Refactors without behavior change — dedupe, naming, structure (with-lingui)."
globs: manage/src/**/*.{ts,tsx}
alwaysApply: false
---

# with-lingui · Refactor（薄规则）

**唯一细则来源**：

`manage/.github/instructions/refactor.instructions.md`

做行为保持式重构时遵守该文件；与 frontend 规则同时命中时，**重构意图**以本文件指向的 refactor 指令为准，其余 UI 细节仍以 `frontend.instructions.md` 为准。

`@manage/.github/instructions/refactor.instructions.md`
