export enum ConfigType {
  INPUT = 'input',
  NUMBER = 'number',
  SWITCH = 'switch',
  SELECT = 'select',
  TEXTAREA = 'textarea',
  COLOR = 'color',
  DATE = 'date',
  JSON = 'json',
  STORAGE = 'storage',
}